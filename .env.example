# Database (Postgres + pgvector)
# Local Docker example; swap for a Neon/Supabase connection string with no code changes.
DATABASE_URL=postgresql://postgres:datum@localhost:5432/postgres

# LLM (Ollama default)
LLM_BASE_URL=http://localhost:11434
LLM_MODEL=llama3.1
EMBED_MODEL=nomic-embed-text
